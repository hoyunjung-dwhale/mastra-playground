# mastra-playground

Mastra 공식 문서(https://mastra.ai/docs)를 절 단위로 읽고 실습하는 학습용 샌드박스다. 스터디 회차별로 다음 순서로 진행한다. Extend 절은 다루지 않는다.

| 회차 | 문서 절 | 범위 |
|---|---|---|
| 7회차 | Build | Agents |
| 8회차 | Build | Memory |
| 9회차 | Develop/Deploy | Storage, Server |
| 10회차 | Observe | Trace + Metrics, Evals |

## 진행 방식

- 명령, 설정 파일, 옵션, 의존성을 실행하거나 추가하기 전에 그것이 무엇인지 먼저 설명하고 진행한다. Mastra 고유 용어(provider, model router, storage, Studio 등)가 처음 나오면 그 자리에서 뜻을 설명한다.
- 사용자는 LLM 일반 개념(에이전트 루프, tool calling, system prompt, 구조화 출력, 스트리밍 등)을 이미 안다. 일반 개념은 한두 줄 리마인드로만 짚고, 설명의 무게는 Mastra가 그것을 어떻게 구현했는지와 실무에서 왜 그렇게 쓰는지에 둔다.
- 문서의 모든 소제목을 다루지 않는다. 실무에서 꼭 쓰는 것만 고르고, 건너뛰는 소제목은 페이지 시작 때 이름과 이유를 한 줄로 알린다.
- 개념이나 API를 설명할 때마다 `/Users/hoyunjung/Developer/clap-agent`에서 같은 것이 어떻게 쓰였는지 코드를 찾아 `파일경로:행`으로 함께 보여 준다. 없으면 없다고 말한다.
- 사용자는 Java·Spring 백엔드 개발자이고 TypeScript를 모른다. 실습에 새 TypeScript 문법이 나오면 그 자리에서 Java와 대응시켜 설명하고, 첫 등장 때는 주석을 단 코드 골격을 보여 준다.
- 실습 파일의 골격(import, 구조, 주석, `// TODO` 표시)은 AI가 만든다. 판단이 들어가는 부분(instructions 문장, 도구 로직, 스키마 필드)은 사용자가 `TODO` 자리를 채운다. AI는 채운 내용을 확인하고 막히는 곳에서 답한다.
- 정리 파일은 회차마다 하나씩 둔다. 저장소 작성자(hoyunjung)는 `docs/` 아래에(7회차 Agents는 `docs/agents.md`), 클론해서 공부하는 다른 사람은 `notes/` 아래에 쓴다. 세션 시작 때 어느 쪽인지 확인한다. 한 회차 안에서는 파일을 나누지 않고 append 한다. 소제목마다 `#### 개념`, `#### 실습 결과`, `#### clap-agent`, `#### 헷갈렸던 지점` 순서로 쓴다. 헷갈렸던 지점에는 사용자가 실제로 되물은 질문을 "질문 → 답 한 줄" 형식으로 남긴다.

## 코드 작성 기준

- 실습 코드여도 운영 코드 기준으로 쓴다. 기준은 Mastra 공식 문서의 권장 방식과 비교 대상 프로젝트(clap-agent)의 관례 순이다.
- 에이전트 id·등록 키 같은 식별자는 `src/mastra/constants.ts`에, 모델 문자열은 `src/mastra/models.ts`에 상수로 둔다. 문자열을 코드 여기저기에 직접 쓰지 않는다.
- system prompt는 `*.prompt.ts` 파일로 분리한다.
- 지켜야 할 제약은 주석이 아니라 타입으로 강제한다. (예: 모델 상수에 `satisfies Record<string, RegisteredModelId>`)
- 주석은 코드를 읽어도 알 수 없는 것(이유, 외부 계약)만 남긴다. 이름과 타입이 이미 말해 주는 내용은 적지 않는다.
- 새 코드를 넣은 뒤에는 `npm run check`(biome)와 `npm run typecheck`를 백그라운드로 돌려 결과만 알린다.

## 커밋과 태그

- 커밋 메시지는 한국어로 쓴다. 형식은 `<절> <소제목> 골격: <내용>` 또는 `<절> <소제목> 완성: <내용>`이다. (예: `Tools 2-1 골격: 할 일 저장소와 추가·조회 도구`)
- 골격 커밋(AI가 만든 `TODO` 포함 파일)과 완성 커밋(사용자가 채운 것)을 나눈다. 다른 사람이 골격 커밋을 checkout 해서 같은 실습을 할 수 있게 하기 위해서다.
- 문서 페이지가 끝나면 `agents/2-tools`처럼 `<절>/<번호>-<페이지>` 태그를 붙이고 README의 태그 표와 "다룬 것과 건너뛴 것" 표를 갱신한다.
- 커밋·푸시·태그는 사용자가 지시할 때 한다.

## 출력 형식

- 설명은 `###` 소제목으로 구역을 나눈다. 구역 하나는 화면 반 정도 분량까지다.
- 한 문단은 두세 문장까지만 둔다. 긴 설명은 불릿으로 쪼개고, 불릿 하나에는 한 문장만 둔다.
- 비교·대응 관계는 표로 쓴다. 파일 참조는 `파일경로:행` 형식으로 불릿 끝에 붙인다.
- 소제목 하나의 설명은 `### 개념` → `### 실습` → `### clap-agent` 순서로 진행한다. 한 번에 한 파트만 내고, 사용자가 "다음"이라고 하면 다음 파트로 넘어간다.
- `### 실습`에는 무엇을 어떤 순서로 하는지 번호 목록으로 쓴다. 단계마다 만들 파일, 넣을 값, 확인 방법을 적는다.
- 구역과 구역 사이, 표와 문단 사이에 빈 줄을 넣는다.
- 채팅 답변의 다이어그램은 터미널에서 보이도록 아스키(코드 블록)로 그린다. mermaid는 `docs/` 정리 파일에만 쓴다.
- 소제목 하나(개념·실습·clap-agent)가 끝날 때마다 페이지별 진행 표와 전체 진행률(%)을 짧게 보여 주고 다음으로 넘어간다.

