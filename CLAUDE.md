# mastra-playground

Mastra 공식 문서(https://mastra.ai/docs)를 절 단위로 읽고 실습하는 학습용 샌드박스다. 스터디 회차별로 다음 순서로 진행한다. Extend 절은 다루지 않는다.

| 회차 | 문서 절 | 범위 |
|---|---|---|
| 7회차 | Build | Agents |
| 8회차 | Build | Memory |
| 9회차 | Develop/Deploy | Storage, Server |
| 10회차 | Observe | Trace + Metrics, Evals |

## 진행 방식

- 명령, 설정 파일, 옵션, 의존성을 실행하거나 추가하기 전에 그것이 무엇인지 먼저 설명하고 진행한다. Mastra 고유 용어(provider, model router, storage, Studio 등)가 처음 나오면 그 자리에서 뜻을 설명한다.
- Mastra API만 설명하지 않고, 그 API가 전제하는 일반 개념(에이전트 루프, tool calling, 컨텍스트 윈도우, 구조화 출력, 스트리밍 등)을 함께 설명한다. 개념이 LLM 호출 흐름의 어느 지점에서 등장하는지와 다른 제품(Claude Code, OpenAI API 등)에서는 어떻게 보이는지를 연결한다.
- 개념이나 API를 설명할 때마다 `/Users/hoyunjung/Developer/clap-agent`에서 같은 것이 어떻게 쓰였는지 코드를 찾아 `파일경로:행`으로 함께 보여 준다. 없으면 없다고 말한다.
- 사용자는 Java·Spring 백엔드 개발자이고 TypeScript를 모른다. 실습에 새 TypeScript 문법이 나오면 그 자리에서 Java와 대응시켜 설명하고, 첫 등장 때는 주석을 단 코드 골격을 보여 준다.
- 실습 파일의 골격(import, 구조, 주석, `// TODO` 표시)은 AI가 만든다. 판단이 들어가는 부분(instructions 문장, 도구 로직, 스키마 필드)은 사용자가 `TODO` 자리를 채운다. AI는 채운 내용을 확인하고 막히는 곳에서 답한다.
- 정리 파일은 회차마다 하나씩 `docs/` 아래에 둔다. (7회차 Agents는 `docs/agents.md`) 한 회차 안에서는 파일을 나누지 않고 append 한다.

## 출력 형식

- 설명은 `###` 소제목으로 구역을 나눈다. 구역 하나는 화면 반 정도 분량까지다.
- 한 문단은 두세 문장까지만 둔다. 긴 설명은 불릿으로 쪼개고, 불릿 하나에는 한 문장만 둔다.
- 비교·대응 관계는 표로 쓴다. 파일 참조는 `파일경로:행` 형식으로 불릿 끝에 붙인다.
- 소제목 하나의 설명은 `### 개념` → `### 실습` → `### clap-agent` 순서로 진행한다. 한 번에 한 파트만 내고, 사용자가 "다음"이라고 하면 다음 파트로 넘어간다.
- `### 실습`에는 무엇을 어떤 순서로 하는지 번호 목록으로 쓴다. 단계마다 만들 파일, 넣을 값, 확인 방법을 적는다.
- 구역과 구역 사이, 표와 문단 사이에 빈 줄을 넣는다.

