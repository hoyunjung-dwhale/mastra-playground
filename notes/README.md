# notes

클론해서 공부하는 사람이 자기 정리를 쓰는 곳이다. `docs/`는 저장소 작성자의 정리이므로 고치지 않는다. 그래야 이후 회차를 `git pull`로 받을 때 충돌이 나지 않는다.

- 파일은 회차마다 하나씩 둔다. (예: `notes/agents.md`)
- 형식은 자유다. `docs/`와 같은 형식을 쓰고 싶으면 그 파일을 복사해서 시작하면 된다.
- 코드는 `study/<이름>` 브랜치에서 `TODO`를 채우고, `git diff master`로 완성 커밋과 비교한다.
